import { View, Text } from "react-native";

export default function PomodoroScreen({ navigation }: any) {
    return (
        <View>
            <Text>Pomodoro Screen</Text>
        </View>
    );
}
