// src/theme/theme.ts

import { DefaultTheme } from "react-native-paper";

const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: "#3bc99eff",
        secondary: "yellow",
    },
};

export default theme;
